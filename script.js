/*const paragrafos = document.querySelectorAll('.drop-dow ul p');

const totalcaracteres = Array.prototype.reduce.call(paragrafos, (acumulador, item) => {
    return acumulador + item.innerText.length;
}, 0);

console.log(totalcaracteres);
console.log(paragrafos);

function createlement(tag, classe, conteudo) {
    const elemento = document.createElement(tag);
    classe ? elemento.classList.add(classe) : null;
    conteudo ? elemento.innerHTML = conteudo : null;
    return elemento;
}
console.log(createlement('li', 'fwef', 'wefwfe'));



const h1title = createlement.bind(null, 'h1', 'titulo');
const wf = h1title('testesdfa', 'fewfw');
console.log(wf);


//verificar dado
function verificardado(dado) {
    return Object.prototype.toString.call(dado);
}
console.log(verificardado('String teste'));


const quadrado = {};
Object.defineProperties(quadrado, {
    lado: {
        value: {
            value: 4,
            enumerable: true,
        }
    }
});
console.log(quadrado);
const configuração = {
    width: 800,
    heigth: 600,
    background: '#fff333'
}
Object.freeze(configuração);*/